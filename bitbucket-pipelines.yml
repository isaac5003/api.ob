image: docker/compose
pipelines:
  default:
    - step:
        name: Build and Test
        services:
          - docker
        caches:
          - node
        script:
          - apt install nodejs
          - docker-compose -f docker-compose.test.yml up -d
          - npm run test:e2e