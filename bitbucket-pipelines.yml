image: docker/compose
pipelines:
  default:
    - step:
        name: Build and Test
        services:
          - docker
        caches:
          - node
        script:
          - apt install nodejs
          - npm install
          - npm run build
          - docker-compose -f docker-compose.local.yml up -d
          - npm run test:e2e